# Lock input for the environment used only for manifest preparation.
# Install requirements-compression.lock, not this file directly.
# LLMLingua loads a separate compressor model; stop it before starting vLLM.
llmlingua==0.2.2
# LLMLingua 0.2.2 expects the legacy tuple-based attention cache.
transformers==4.36.2
tokenizers==0.15.2

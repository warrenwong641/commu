# Copy to .env and fill locally. Never commit the resulting file.
OPENROUTER_API_KEY=
GEMINI_API_KEY=

# The local vLLM endpoint normally does not require a real secret.
LOCAL_VLLM_API_KEY=local-test-key

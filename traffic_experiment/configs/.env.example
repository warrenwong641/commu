# Do not copy credentials into this or another project configuration file.
# Export only the credential needed by the current process:
#
#   read -rsp "API key: " LOCAL_VLLM_API_KEY
#   printf '\n'
#   export LOCAL_VLLM_API_KEY
#
# External validation uses OPENROUTER_API_KEY or GEMINI_API_KEY in the same
# process-only manner. Unset each variable when the run is complete.

# LLM Traffic Measurement Experiment

This directory contains a plan and an executable local-server runner for measuring network traffic generated by
the same LoCoMo prompts under three inference backends:

1. a local Qwen3-8B model served by vLLM;
2. Qwen3-8B through OpenRouter;
3. a direct external API, initially Gemini 3.5 Flash-Lite.

It does not modify the existing `locomo_eval` implementation. The executable path
currently targets local vLLM; the frozen request manifest is backend-neutral so the
external API adapters can reuse it later.

## Recommended execution order

1. Freeze the 32 LoCoMo samples and precompute LongLLMLingua prompts for no
   compression, 2x compression, and 4x compression.
2. Run the 8-sample pilot against local vLLM.
3. Validate capture completeness, token accounting, and timestamps.
4. Run the same pilot through one pinned OpenRouter provider and the direct Gemini API.
5. Only then run the 32-sample main experiment.

Local vLLM should be the primary controlled experiment. External APIs should be
reported as validation environments because provider infrastructure, routing,
TLS records, and server-side behavior are not controlled by the researcher.

## Directory map

- `configs/experiment_plan.yaml`: editable assumptions and backend settings.
- `configs/.env.example`: variable names only; never commit real API keys.
- `docs/architecture.md`: client/server and packet-capture design.
- `docs/run_protocol.md`: reproducible pilot and main-run procedure.
- `docs/cost_time_estimates.md`: formulas and numerical estimates.
- `docs/local_server_handoff.md`: copy-to-server installation and run commands.
- `schemas/run_manifest.schema.json`: minimum metadata for every measured request.
- `traffic_measure/`: manifest preparation, vLLM runner, capture, and analysis code.
- `scripts/`: Linux batch-equivalent setup and execution commands.
- `server.env.example`: server-specific blanks and defaults.

## Profiles

| Profile | Samples | Conditions | Repetitions | Calls/backend |
|---|---:|---:|---:|---:|
| Pilot | 8 | 3 | 3 | 72 |
| Main | 32 | 3 | 5 | 480 |
| Robustness | 32 | 3 | 10 | 960 |

The estimates assume an average of 9,000 input tokens before compression and
128 output tokens. With equally represented no-compression, 2x, and 4x conditions,
the weighted average is 5,250 input tokens per call.

## Reproducibility rules

- Keep dataset sample IDs, prompt text, system prompt, generation parameters, and
  condition order identical across backends.
- Pin the OpenRouter model and provider; disable provider fallback.
- Use a stable Gemini model identifier rather than a `latest` alias.
- Run measured requests serially so traffic from different trials cannot overlap.
- Keep compression outside the measurement window and reuse the same compressed
  prompt for repetitions.
- Record application timestamps and packet timestamps in UTC.
- Store packet captures outside version control.
- Recheck provider prices and rate limits immediately before the final run.

Pricing in this plan was checked on 2026-08-03 and is deliberately stored as
configuration rather than embedded in experiment code.
